version: '3.7'

services:
  application:
    image: application
    build:
      context: .
      target: development
    ports:
      - 5000:5000
    volumes:
      - ./:/app
    environment:
      - FLASK_APP=autoapp.py
      - FLASK_DEBUG=1
      - FLASK_ENV=development